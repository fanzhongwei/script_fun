opencv-python>=4.8.0,<6.0.0
easyocr>=1.7.0,<2.0.0
Pillow>=10.0.0,<13.0.0
PyYAML>=6.0,<7.0
numpy>=1.24.0,<3.0.0
simple-lama-inpainting>=0.1.0,<0.2.0
PySide6>=6.6.0,<7.0.0

# GUI 含 PySide6；PyTorch 由 ensure-dev-env.sh / run-gui.sh 按 GPU 与 python3-dev 版本安装：
#   NVIDIA → 探测该解释器可用的 CUDA 源（cu124/cu126/…）；AMD → ROCm；其他 → CPU
