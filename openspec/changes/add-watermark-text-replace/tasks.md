# Tasks

## 1. 字体与替换算法

- [ ] 1.1 在 `python/watermark_remover/fonts/` 放入黑体、宋体两份 OFL 子集，并让加载函数在开发目录与打包目录都能找到它们。验证：无系统中文字体时，`python3-dev` 仍能用这两份文件创建 Pillow 字体对象
- [ ] 1.2 新增文案框数据（矩形、目标文案、字体）和单框替换函数：框内 OCR、字形 mask、LaMa、按中心/高度/字形中位色绘制、过长则缩小到框内。验证：用一张带单行深色字的合成图调用该函数，输出尺寸与输入相同，且目标句子的像素落在原文字矩形内
- [ ] 1.3 识别失败时不抛错，退化为整块用户矩形估计字号，文案留空待填。验证：对空白矩形调用识别，返回空文案且不异常

## 2. 接入现有预览与导出

- [ ] 2.1 `ImageTask` 增加擦除/替换模式和 `text_boxes`，与 `regions` 分开；改框、改文案、改字体、切换模式时作废预览与确认。验证：改文案后 `is_exportable()` 为假，且擦除 `regions` 未被文案框写入
- [ ] 2.2 缩略预览只处理非空文案框，导出时按 `scale` 在原图重做再走现有 `apply_export_profile`。验证：原图导出文件的替换发生在缩放前的原图尺寸上，且擦除模式导出函数签名与行为保持可用
- [ ] 2.3 矩形提交后在现有 worker 中做框内识别并回填，选图加载缩略图时不调用识别。验证：只加载图片时 worker 不发起 OCR；提交矩形后目标文案被回填或明确为空

## 3. 界面

- [ ] 3.1 主窗口增加「擦除 / 替换文案」切换。替换模式下只能画矩形，可编辑当前框的文案和黑体/宋体；空文案不能确认。验证：擦除模式仍可画椭圆并生成并集预览；替换模式空文案时确认按钮不可用
- [ ] 3.2 多框可分别编辑，生成预览、确认、导出沿用现有按钮并按模式分支。验证：同一张图两块框填入不同句子后，一次预览两处都被替换，确认后「导出当前」可用

## 4. 说明与打包

- [ ] 4.1 更新 `python/watermark_remover/README.md`：替换模式操作、字体选择、预览确认导出，以及只保证位置、大小、颜色接近。验证：文档含上述四类说明
- [ ] 4.2 `packaging/watermark_remover_gui.spec` 把 `fonts/` 打进 datas，不新增 requirements、不改模型下载脚本。验证：spec 的 datas 含字体目录，`requirements.txt` 无新增依赖
