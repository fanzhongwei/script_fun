## 1. 目录与脚本骨架

- [x] 1.1 创建 `tampermonkey/price_calculator/` 目录
- [x] 1.2 新增 `price_calculator.user.js`，编写 Tampermonkey 元数据头（`@match *://mms.pinduoduo.com/*`、`GM_setValue`/`GM_getValue` 等 `@grant`）
- [x] 1.3 注入 `#pc-root` 容器、样式隔离与 80vw×80vh 弹窗基础 CSS
- [x] 1.4 实现可拖动 FAB「价格计算」（复用 image_exporter 的拖动与位置持久化模式）

## 2. SKU 表格扫描

- [x] 2.1 实现表头语义匹配（「款式」「拼单价」「单买价」列定位）
- [x] 2.2 提取每行 `{ style, groupInput, singleInput, rowIndex }` 并过滤空行
- [x] 2.3 处理未找到表格时的空状态提示

## 3. 弹窗表格 UI

- [x] 3.1 实现左 3 列固定（款式、成本、目标销售利润率）+ 中间可滚动 + 右 1 列固定布局
- [x] 3.2 渲染中间列：运费、退货率、利润率（只读）、平台扣点
- [x] 3.3 渲染右侧表头：全局活动区（5 个立减券 + 时限量购）+ 下方唯一「回填」按钮；表头第二行「拼单价」「单买价」；数据行仅展示计算结果
- [x] 3.4 实现「率」类字段 0～100 百分比输入与钳制校验
- [x] 3.5 初始化默认值（运费 0、退货率 20%、目标销售利润率 20%、平台扣点 0.6%），活动默认不勾选

## 4. 计算引擎

- [x] 4.1 实现基础公式（含平台扣点）：`N = (C+E)/((1-I)-J/(1-G))`，`K = ROUND(N,2)`，`L = ROUND(K×1.2+5,2)`，`F = N×(1-I)-C-E`
- [x] 4.2 实现五种互斥活动的 Excel 公式候选计算（订单复购券、店铺关注券、新客立减券、直播券、场景券）
- [x] 4.3 实现互斥活动取拼单价最高的一组结果
- [x] 4.4 实现时限量购叠加（立减 / 打折两种类型）
- [x] 4.5 实现利润率只读回算与展示（`(F/N)×(1-G)`，F 含平台扣点，百分比显示）
- [x] 4.6 绑定全局活动与各行 input 变更事件，触发实时重算

## 5. 成本批量粘贴

- [x] 5.1 监听成本列 paste 事件，按常见分隔符拆分并向下填充
- [x] 5.2 超出剩余行数时终止填充并 toast 提示丢弃数量
- [x] 5.3 粘贴完成后自动重算受影响行

## 6. 回填原页面

- [x] 6.1 在活动区下方实现唯一「回填」按钮，调用 `fillBackAll()` 一次性写入所有有效行
- [x] 6.2 使用 native value setter + input/change 事件；按扫描 DOM 引用与款式文本校验行列对应
- [x] 6.3 汇总成功/跳过/失败数量并 toast 反馈

## 7. 文档与自检

- [x] 7.1 编写 `tampermonkey/price_calculator/README.md`（环境依赖、脚本参数、使用配置、FAQ）
- [x] 7.2 验证基础公式样例（平台扣点 0% 时：成本 5.94 → 拼单价 7.92、单买价 14.50；平台扣点 0.6% 时拼单价升高）
- [x] 7.3 在拼多多商家后台页面手动验证：扫描、计算、回填全流程
- [x] 7.4 自检：不改动既有 linux/python/image_exporter 脚本
